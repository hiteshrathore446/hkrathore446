import 'package:flutter/material.dart';

class color_page extends StatefulWidget {
  const color_page({super.key});

  @override
  State<color_page> createState() => color_pageState();
}

class color_pageState extends State<color_page> {
  @override
 // late int index = 0;
 // void changecolors() {
    // setState(() => index = rendom.nextInt(4));
 // }

 // List Colours = [Colors.red, Colors.green];
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
